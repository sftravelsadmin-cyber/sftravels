# SF Travels - Premium Travel Booking Website

A modern, high-converting, premium travel booking and lead-generation website built with **Astro (v4)**, **React**, and **Vanilla CSS**.

---

## 🚦 MANDATORY AGENT INSTRUCTIONS (READ FIRST!)

If you are an AI assistant starting a new session on this repository, you **MUST** follow these rules:

1. **Read Context Immediately**: First, read [project_context.md](file:///e:/softwares/sftravels/project_context.md) and the agentic skill rules in [.gemini/skills/project_rules.md](file:///e:/softwares/sftravels/.gemini/skills/project_rules.md).
2. **Build and Git Constraints**:
   - **DO NOT** run `npm run build` unless explicitly asked by the user.
   - **DO NOT** push to GitHub unless explicitly asked by the user.
   - **Pre-Push Verification**: When asked to push to GitHub, you **MUST** run the build command (`npm run build`) first to verify that static site compilation succeeds with zero errors.

---

## 🛠️ Local Development

### Prerequisites
A portable Node.js v20.11.1 environment is available. To execute commands under PowerShell, prepend the local Node path:
```powershell
$env:PATH = 'C:\Users\user2\node\node-v20.11.1-win-x64;' + $env:PATH
```

### Start Development Server
```powershell
npm run dev
```
